Metadata-Version: 2.1
Name: omniops
Version: 0.1.0
Summary: OmniOps — 结构化数据驱动的智能诊断与建议系统
Requires-Python: >=3.8
Description-Content-Type: text/markdown
Requires-Dist: fastapi>=0.104.0
Requires-Dist: uvicorn[standard]>=0.24.0
Requires-Dist: pydantic>=2.5.0
Requires-Dist: pydantic-settings>=2.1.0
Requires-Dist: python-multipart>=0.0.6
Requires-Dist: pandas>=2.0.0
Requires-Dist: openpyxl>=3.1.0
Requires-Dist: qdrant-client>=1.7.0
Requires-Dist: neo4j>=5.15.0
Requires-Dist: redis>=5.0.0
Requires-Dist: sqlalchemy[asyncio]>=2.0.0
Requires-Dist: asyncpg>=0.29.0
Requires-Dist: anthropic>=0.21.0
Requires-Dist: python-jose[cryptography]>=3.3.0
Requires-Dist: structlog>=23.0.0
Requires-Dist: httpx>=0.25.0
Requires-Dist: tenacity>=8.2.0
Requires-Dist: python-dateutil>=2.8.0
Requires-Dist: chardet>=5.2.0
Provides-Extra: ocr
Requires-Dist: paddlepaddle>=2.5.0; extra == "ocr"
Requires-Dist: paddleocr>=2.7.0; extra == "ocr"
Requires-Dist: pillow>=10.0.0; extra == "ocr"
Requires-Dist: pdf2image>=1.16.0; extra == "ocr"
Provides-Extra: dev
Requires-Dist: pytest>=7.4.0; extra == "dev"
Requires-Dist: pytest-asyncio>=0.21.0; extra == "dev"
Requires-Dist: pytest-cov>=4.1.0; extra == "dev"
Requires-Dist: httpx>=0.25.0; extra == "dev"
Requires-Dist: ruff>=0.1.0; extra == "dev"
Requires-Dist: mypy>=1.7.0; extra == "dev"
Requires-Dist: pre-commit>=3.5.0; extra == "dev"
Requires-Dist: types-python-dateutil; extra == "dev"
Requires-Dist: types-redis; extra == "dev"

# OmniOps

结构化数据驱动的智能诊断与建议系统

## 功能特性

- **CSV/OCR 摄取**：支持 CSV 文件上传和图片/PDF 表格 OCR 提取
- **MultiAgent 协作**：感知 → 诊断 → 影响 → 方案 → 验证的完整链路
- **RAG 知识库**：基于历史案例的智能根因分析
- **人机协同**：高危方案人工审核，知识库持续学习

## 快速开始

### 前置依赖

- Python 3.8+
- Docker & Docker Compose（用于基础设施服务）

### 安装

```bash
pip install -e .
```

### 启动基础设施

```bash
docker-compose up -d
```

### 启动服务

```bash
omniops
# 或直接运行
python -m omniops.api.main
```

服务运行在 http://localhost:8000

### API 文档

- Swagger UI: http://localhost:8000/docs
- ReDoc: http://localhost:8000/redoc

## 开发

### 安装开发依赖

```bash
pip install -e ".[dev]"
```

### 运行测试

```bash
pytest
```

### 代码检查

```bash
ruff check src/ tests/
mypy src/
```

### Pre-commit Hooks

```bash
pre-commit install
pre-commit run --all-files
```

## 项目结构

```
src/omniops/
├── agents/        # Agent 实现（感知、诊断、影响、方案）
├── api/           # FastAPI 路由和入口
├── core/          # 核心工具（配置、编码检测）
├── ingestion/     # 数据摄取（CSV 解析、OCR）
├── memory/        # 会话存储
├── models/        # Pydantic 数据模型
├── rag/           # RAG 知识库
└── router/        # 上下文路由
```

## API 示例

### 创建诊断会话

```bash
curl -X POST http://localhost:8000/v1/sessions \
  -F "file=@alarms.csv"
```

### 获取诊断结果

```bash
curl http://localhost:8000/v1/sessions/{session_id}/result
```

### 提交反馈

```bash
curl -X POST http://localhost:8000/v1/sessions/{session_id}/feedback \
  -H "Content-Type: application/json" \
  -d '{"decision": "adopted", "actual_action": "已更换光模块", "effectiveness": "resolved"}'
```
